version: '3.1'
services:
  db:
    image: mysql
    container_name: mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
    ports:
    - "3307:3306"
    volumes:
    - netprecision_data:/var/lib/mysql

volumes:
  netprecision_data:
